
@{
    Layout = null;
}



@using (Html.BeginForm("Process", "Activate"))
{
    @Html.AntiForgeryToken()


    @Html.DevExpress().FormLayout(settings =>
{
    settings.Name = "NewCompany";
    settings.Width = Unit.Pixel(500);
    settings.ColCount = 2;

    settings.Items.Add(itemSettings =>
    {
        itemSettings.CaptionCellStyle.CssClass = "CaptionWidth";

        itemSettings.Name = "LicenseKey";
        itemSettings.NestedExtensionType = FormLayoutNestedExtensionItemType.TextBox;

        var tb = (TextBoxSettings)itemSettings.NestedExtensionSettings;
        tb.Width = 300;
        tb.Properties.NullText = "Activate by enter license-key.";
        itemSettings.ShowCaption = DefaultBoolean.False;
        //  itemSettings.Width = 300;
    });

    settings.Items.Add(item =>
    {
        item.NestedExtensionType = FormLayoutNestedExtensionItemType.Button;
        item.ShowCaption = DefaultBoolean.False;
        item.HorizontalAlign = FormLayoutHorizontalAlign.Right;
        var button = (ButtonSettings)item.NestedExtensionSettings;
        button.Name = "ActivateLicenseButton";
        button.Text = "Next";
        button.UseSubmitBehavior = true;
    });
}).GetHtml()


}