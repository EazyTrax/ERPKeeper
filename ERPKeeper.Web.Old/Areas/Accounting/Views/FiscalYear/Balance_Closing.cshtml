@model ERPKeeper.Node.Models.Accounting.FiscalYear

<div class="globalContainer">
  <div class="row">
    <div class="col-12 col-Middle p-0">
      @Html.Partial("_MenuTop")

      @using (Html.BeginForm("ExportTo"))
      {
        @Html.Action("PartialGridView_Fiscal_Closing_Balance", new { Id = Model.Uid })
      }
    </div>
  </div>
</div>