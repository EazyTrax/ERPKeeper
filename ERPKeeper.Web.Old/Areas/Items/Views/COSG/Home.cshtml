


<div class="globalContainer">
    <div class="row">
        <div class="col-2 col-Left pl-0">
            @Html.Action("_Main", "Default", new { Area = "Menu" })
        </div>
        <div class="col col-Middle p-0" style="">
            @Html.Partial("_MenuTop")

            @using (Html.BeginForm("ExportTo"))
            {
                @Html.Action("PartialGridView", new { id = ViewBag.Id })
            }
        </div>
    </div>
</div>


