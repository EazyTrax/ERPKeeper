@using Microsoft.AspNetCore.Http
@using System.Security.Claims;


@if (User.Identity.IsAuthenticated)
{
    var makerId = this.User.FindFirstValue(ClaimTypes.NameIdentifier);


    <div class="flex justify-end gap-2">
        <partial name="_DropMenu_Edit_Mode" />
        <partial name="_DropMenu_Signout" />

    </div>
}