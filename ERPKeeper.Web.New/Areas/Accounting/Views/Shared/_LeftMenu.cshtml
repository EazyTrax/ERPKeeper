



@{
    void MenuTemplate(int level, string displayName, string url, int? count = null)
    {
        <div >
            <div class="flex  justify-between e-r">
                <div class="w-10/12 p-0">
                    <a href="@url">
                        @if (level > 0)
                        {
                            
                        }
                        @SharedLocalizer.GetLocalizedHtmlString(displayName)
                    </a>
                </div>
                <div class="w-2/12 text-right p-0">
                    <span class="menuCount">@count</span>
                </div>
            </div>
        </div>
    }
}


<div class="lh-120 bg-gray-100 font-semibold p-2 rounded border">
@{
    MenuTemplate(0, "Accounting", $"/Accounting");
}
</div>
<div class="lh-120 bg-gray-100 p-2 rounded border">
    @{

        MenuTemplate(0, "Accounts", $"/Accounting/Accounts", Organization.ErpCOREDBContext.Accounts.Count());
        MenuTemplate(1, "Default Accounts", $"/Accounting/DefaultAccounts");
        MenuTemplate(0, "Fiscal Years", $"/Accounting/FiscalYears", Organization.ErpCOREDBContext.FiscalYears.Count());
        MenuTemplate(0, "Journal Entries", $"/Accounting/JournalEntries", Organization.ErpCOREDBContext.JournalEntries.Count());
        MenuTemplate(1, "Journal Types", $"/Accounting/JournalEntryTypes");
        MenuTemplate(0, "Transactions", $"/Accounting/Transactions");
        MenuTemplate(0, "Opening", $"/Accounting/OpeningBalances");

    }
</div>
