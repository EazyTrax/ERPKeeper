@model ERPKeeperCore.Enterprise.Models.Suppliers.PurchaseQuote


@{
    void MenuTemplate(int level, string displayName, string url, int? count = null)
    {
        <div>
            <div class="flex  justify-between e-r">
                <div class="w-10/12 p-l@(level)">
                    <a href="@url">
                        @if (level > 0)
                        {

                        }
                        @SharedLocalizer.GetLocalizedHtmlString(displayName)
                    </a>
                </div>
                <div class="w-2/12 text-right p-0">
                    <span class="menuCount">@count</span>
                </div>
            </div>
        </div>
    }
}


<div class="lh-120 bg-gray-100 p-2 rounded border font-semibold">
    <a href="/Suppliers/PurchaseQuotes/@Model.Id">@Model.Name</a>
</div>

<div class="lh-120 bg-gray-100 p-2 rounded border">
    @{
        MenuTemplate(0, "Detail", $"/Suppliers/PurchaseQuotes/{Model.Id}");
        MenuTemplate(0, "Items", $"/Suppliers/PurchaseQuotes/{Model.Id}/Items", Model.Items.Count());
        MenuTemplate(0, "Documents", $"/Suppliers/PurchaseQuotes/{Model.Id}/Documents");
    }
</div>

<div class="lh-120 bg-gray-100 p-2 rounded border">
    @{
        MenuTemplate(0, "Export", $"/Suppliers/PurchaseQuotes/{Model.Id}/Export");
    }
</div>
