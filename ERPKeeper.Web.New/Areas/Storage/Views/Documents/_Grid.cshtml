
<div id="treeList" class="">

    @(Html.DevExtreme().DataGrid<ERPKeeperCore.Enterprise.Models.Storage.Document>()
            .ID("gridList")
            .DataSource(d => d.RemoteController().Key("Id")
            .LoadUrl($"/API/Storage/Documents/All")
            .InsertUrl($"/API/Storage/Documents/Insert")
            .UpdateUrl($"/API/Storage/Documents/Update")
            .UpdateMethod("Post")
            .DeleteUrl($"/API/Storage/Documents/Delete")
            .DeleteMethod("Post")
            )
            .KeyExpr("Id")
            .Width("100%")
            .WordWrapEnabled(true)
            .RowAlternationEnabled(true).AllowColumnReordering(true)
            .Editing(editing => editing.Mode(GridEditMode.Row).AllowUpdating(IsOnEditMode).AllowAdding(IsOnEditMode).UseIcons(true))
            .StateStoring(stateStoring => stateStoring.Enabled(true).Type(StateStoringType.LocalStorage).StorageKey("Documents"))

            .ShowRowLines(true)
            .ShowBorders(false)
            .Paging(paging => paging.PageSize(100))
            .AllowColumnResizing(true).AllowColumnReordering(true)
            .HeaderFilter(headerFilter => headerFilter.Visible(true).Search(s => s.Enabled(true)))
            .ColumnAutoWidth(false)
            .Columns(column =>
            {


             column.AddFor(m => m.FileSizeString)
            .Width(100)
            .Caption("File Size")
            .SortIndex(0)
            .SortOrder(SortOrder.Desc)
            .AllowEditing(false)
            .CalculateSortValue("Size");

                column.AddFor(m => m.FileName)
            .HeaderCellTemplate("Name")
            .CellTemplate(new JS("function(container, options) { $('<a>').attr('href', '/storage/documents/' + options.data.Id + '/').text(options.data.FileName).appendTo(container); }"));


       
                column.AddFor(m => m.FileExtension).AllowEditing(false)
            .HeaderCellTemplate("Ext");

                column.AddFor(m => m.ProfileId)
            .Lookup(lookup => lookup
            .DataSource(Organization.ErpCOREDBContext.Profiles.Select(i => new { Value = i.Id, Text = i.Name }).ToList())
            .DisplayExpr("Text")
            .ValueExpr("Value"))
            .Width(150)
            
            .HeaderCellTemplate(@<text>Profile</text>)
        .CellTemplate(new JS("function(container, options) { $('<a>').attr('href', '/Profiles/Profile/' + options.data.ProfileId + '/').text(options.displayValue).appendTo(container); }"));




                column.AddFor(m => m.AgeAgo)
            .Width(100)
            .Caption("File Size")
            .SortIndex(0)
            .SortOrder(SortOrder.Desc)
            .AllowEditing(false)
            .CalculateSortValue("UploadDate");


            column.AddFor(m => m.Note)
                .HeaderCellTemplate("Note")
                .CellTemplate(new JS("function(container, options) { $('<a>').attr('href', '/storage/documents/' + options.data.Id + '/').text(options.data.Name).appendTo(container); }"));



            column.Add()
            .Width(40)
            .AllowSearch(false)
            .AllowEditing(false)
            .CellTemplate(new JS("function(container, options) { $('<a>').attr('href', '/storage/documents/' + options.data.Id + '/Download').append($('<i>').addClass('fa-solid fa-cloud-arrow-down text-fs-15')).appendTo(container); }"));


            })
        )
</div>


