@model ERPKeeperCore.Enterprise.Models.Customers.ReceivePayment


@{
    void MenuTemplate(int level, string displayName, string url, int? count = null)
    {
        <div>
            <div class="flex e-r">
                <div class="w-10/12 p-l@(level)">
                    <a href="@url">
                        @if (level > 0)
                        {
                            
                        }
                        @SharedLocalizer.GetLocalizedHtmlString(displayName)
                    </a>
                </div>
                <div class="w-2/12 p-0 text-right p-0">
                    <span class="menuCount">@count</span>
                </div>
            </div>
        </div>
    }
}


<div class="lh-120 bg-gray-100 p-2 rounded border font-weight-600">
    <a href="/@CompanyId/Customers/ReceivePayments">ReceivePayments</a>:
    <a href="/@CompanyId/Customers/ReceivePayments/@Model.Id">@Model.Name</a>
</div>

<div class="lh-120 bg-gray-100 p-2 rounded border">
    @{
        MenuTemplate(0, "Detail", $"/{CompanyId}/Customers/ReceivePayments/{Model.Id}");
        MenuTemplate(0, $"Sale: {Model.Sale.Name}", $"/{CompanyId}/Customers/Sales/{Model.SaleId}");
        MenuTemplate(0, "Export", $"/{CompanyId}/Customers/ReceivePayments/{Model.Id}/Export");
    }
</div>
