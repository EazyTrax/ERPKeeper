@{
    void MenuTemplate(int level, string displayName, string url, int? count = null)
    {
        <div>
            <div class="row e-r">
                <div class="col-10 p-l@(level)">
                    <a href="@url">
                        @if (level > 0)
                        {
                            <i class="fa-solid fa-l"></i>
                        }
                        @SharedLocalizer.GetLocalizedHtmlString(displayName)
                    </a>
                </div>
                <div class="col-2 text-right p-0">
                    <span class="menuCount">@count</span>
                </div>
            </div>
        </div>
    }
}


<div class="lh-12 bg-fb1 p-2 rounded border font-weight-600">
    @{
        MenuTemplate(0, "Profiles", $"/{CompanyId}/Profiles");
    }
</div>

<div class="lh-12 bg-fb1 p-2 rounded border">
    @{
        MenuTemplate(0, "Customers", $"/{CompanyId}/Profiles/Customers", Organization.Customers.Count());
        MenuTemplate(0, "Suppliers", $"/{CompanyId}/Profiles/Suppliers", Organization.Suppliers.Count());
        MenuTemplate(0, "Employees", $"/{CompanyId}/Profiles/Employees", Organization.Employees.Count());
        MenuTemplate(0, "Investors", $"/{CompanyId}/Profiles/Investors", Organization.Investors.Count());
    }
</div>

<div class="lh-12 bg-fb1 p-2 rounded border">
    @{
        MenuTemplate(0, "Suppliers", $"/{CompanyId}/Profiles/Suppliers/Suppliers", Organization.Customers.Count());
        MenuTemplate(0, "Quotes", $"/{CompanyId}/Profiles/Suppliers/Quotes");
        MenuTemplate(0, "Purchases", $"/{CompanyId}/Profiles/Suppliers/Purchases", Organization.ErpCOREDBContext.Purchases.Count());
        MenuTemplate(1, "Payments", $"/{CompanyId}/Profiles/Suppliers/SupplierPayments", Organization.ErpCOREDBContext.SupplierPayments.Count());
    }
</div>