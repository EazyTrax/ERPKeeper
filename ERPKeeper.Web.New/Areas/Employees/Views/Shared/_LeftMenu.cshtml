@{
    void MenuTemplate(int level, string displayName, string url, int? count = null)
    {
        <div>
            <div class="flex  justify-between e-r">
                <div class="w-10/12 p-l@(level)">
                    <a href="@url">
                        @if (level > 0)
                        {

                        }
                        @SharedLocalizer.GetLocalizedHtmlString(displayName)
                    </a>
                </div>
                <div class="w-2/12 text-right p-0">
                    <span class="menuCount">@count</span>
                </div>
            </div>
        </div>
    }
}

<div class="lh-120 bg-gray-100 p-2 rounded border font-semibold">
    <a href="/Employees">Employees</a>
</div>


<div class="lh-120 bg-gray-100 p-2 rounded border">
    @{
        MenuTemplate(0, "Positions", $"/Employees/EmployeePositions", Organization.ErpCOREDBContext.EmployeePositions.Count());
        MenuTemplate(0, "Employees", $"/Employees/Employees", Organization.Employees.Count());
    }
</div>


<div class="lh-120 bg-gray-100 p-2 rounded border">
    <div class="font-semibold ">
        Payments
    </div>
    <div class="">
        @{
            MenuTemplate(1, "Types", $"/Employees/EmployeePaymentTypes", Organization.ErpCOREDBContext.EmployeePaymentTypes.Count());
            MenuTemplate(1, "Periods", $"/Employees/EmployeePaymentPeriods", Organization.ErpCOREDBContext.EmployeePaymentPeriods.Count());
            MenuTemplate(0, "Payments", $"/Employees/EmployeePayments", Organization.ErpCOREDBContext.EmployeePayments.Count());
        }
    </div>
</div>