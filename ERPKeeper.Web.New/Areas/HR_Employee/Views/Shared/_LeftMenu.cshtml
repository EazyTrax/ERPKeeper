@{
    void MenuTemplate(int level, string displayName, string url, int? count = null)
    {
        <div>
            <div class="flex  justify-between e-r">
                <div class="w-10/12 p-l@(level)">
                    <a href="@url">
                        @if (level > 0)
                        {

                        }
                        @SharedLocalizer.GetLocalizedHtmlString(displayName)
                    </a>
                </div>
                <div class="w-2/12 text-right p-0">
                    <span class="menuCount">@count</span>
                </div>
            </div>
        </div>
    }
}

<div class="lh-120 bg-gray-100 p-2 rounded border font-semibold">
    <a href="/HR/Employees">Employees</a>
</div>


<div class="lh-120 bg-gray-100 p-2 rounded border">
    @{
        MenuTemplate(0, "Positions", $"/HR/Employees/EmployeePositions", Organization.ErpCOREDBContext.EmployeePositions.Count());
        MenuTemplate(0, "Employees", $"/HR/Employees", Organization.Employees.Count());
    }
</div>


<div class="lh-120 bg-gray-100 p-2 rounded border">
    <div class="font-semibold ">
        Payments
    </div>
    <div class="">
        @{
            MenuTemplate(1, "Types", $"/HR/Employees/EmployeePaymentTypes", Organization.ErpCOREDBContext.EmployeePaymentTypes.Count());
            MenuTemplate(1, "Periods", $"/HR/Employees/EmployeePaymentPeriods", Organization.ErpCOREDBContext.EmployeePaymentPeriods.Count());
            MenuTemplate(0, "Payments", $"/HR/Employees/EmployeePayments", Organization.ErpCOREDBContext.EmployeePayments.Count());
      
        
        
        }
    </div>
</div>